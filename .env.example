# Engine HTTP API
#VITE_API_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=https://portal.stpis.edu.gh/id-engine

# Optional: X-API-Key for /api/users, /api/deposits, etc. (not /api/portal/*)
# Create a key in the dev portal, attach to an app, then paste the raw key here and restart Vite.
# Or without restart: in the browser devtools console run:
#   localStorage.setItem('ie_dev_identity_api_key', 'YOUR_RAW_KEY');
VITE_IDENTITY_ENGINE_API_KEY=ie_live_LN6V7SknCRvW8LhKjYj427vm
